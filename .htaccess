<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /web-desa-ci3/

    # Jika file atau folder tidak ada, lempar ke index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>
